;;;; This software outputs a string to the console

(format t "Hello Lisp ~%") ; `t` indicates the terminal usage, `~%` is a newline

;;; printing out to the console with print
(print "This is being printed with (print )")

